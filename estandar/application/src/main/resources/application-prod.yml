management:
  server:
    port: 8081
  endpoint:    prometheus:      enabled: true      
  metrics:
#    web:#      server:#        auto-time-requests: false    export:      
      prometheus:        enabled: true               endpoints:
    web:
      base-path: "/manage"        
      exposure:
        include: "*" # "health,info"
                  
logging:
    level:
        ROOT: INFO        
        com.comerzzia.api: INFO
        org.mybatis: INFO   
        org.springframework : INFO 